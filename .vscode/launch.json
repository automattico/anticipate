{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "monkeyc",
      "request": "launch",
      "name": "Run App: Choose Device Each Run",
      "stopAtLaunch": false,
      "device": "${command:GetTargetDevice}"
    },
    {
      "type": "monkeyc",
      "request": "launch",
      "name": "Run Native Pairing: Choose Device Each Run",
      "runNativePairing": true,
      "stopAtLaunch": false,
      "device": "${command:GetTargetDevice}"
    }
  ]
}
